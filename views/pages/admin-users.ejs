<div class="max-w-4xl mx-auto">
    <% if (!user || user.role !== 'admin') { %>
        <div class="text-center py-12">
            <h1 class="text-4xl font-bold text-gray-900 mb-4">Erişim Reddedildi</h1>
            <p class="text-xl text-gray-600 mb-8">Bu sayfayı görüntülemek için admin yetkisine sahip olmanız gerekiyor</p>
            <div class="space-x-4">
                <a href="/" class="inline-block bg-blue-500 text-white px-6 py-3 rounded-lg hover:bg-blue-600">Ana Sayfaya Dön</a>
            </div>
        </div>
    <% } else { %>
        <div class="mb-6">
            <h1 class="text-3xl font-bold text-gray-100 mb-4">Kullanıcı Yönetimi</h1>
            <p class="text-gray-300">Tüm kullanıcıları görüntüleyin ve admin yetkisi atayın</p>
        </div>

        <!-- Bilgi Mesajı -->
        <div class="bg-blue-50 border-l-4 border-blue-500 p-4 mb-6 text-blue-700 rounded">
            <div class="flex">
                <div class="flex-shrink-0">
                    <svg class="h-5 w-5 text-blue-500" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
                    </svg>
                </div>
                <div class="ml-3">
                    <p class="text-sm">Admin yetkisi, kullanıcıların takım oluşturma, düzenleme ve yönetme yetkisini verir. Lütfen bu yetkiyi dikkatli bir şekilde verin.</p>
                </div>
            </div>
        </div>

        <!-- Kullanıcılar Listesi -->
        <div class="bg-white rounded-lg shadow p-6">
            <h2 class="text-2xl font-semibold text-gray-900 mb-4">Tüm Kullanıcılar</h2>
            
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Kullanıcı</th>
                            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">E-posta</th>
                            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Rol</th>
                            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Kayıt Tarihi</th>
                            <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200" id="users-table-body">
                        <tr>
                            <td colspan="5" class="px-6 py-4 text-center text-gray-500">
                                <p>Kullanıcılar yükleniyor...</p>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Kullanıcı bilgilerini body'e veri özniteliği olarak ekle -->
        <script>
            document.body.setAttribute('data-user-id', '<%= user._id %>');
        </script>
        
        <!-- Harici JavaScript dosyasını ekle -->
        <script src="/js/admin-users.js"></script>
    <% } %>
</div> 